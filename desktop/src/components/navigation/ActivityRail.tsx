import type {NavigationView} from '../../types/desktop'; import {useLocale} from '../../i18n/LocaleContext'; import {Icon} from '../ui/Icon'; export function ActivityRail({active,onSelect}:{active:NavigationView;onSelect:(v:NavigationView)=>void}){const {t}=useLocale();return <nav className="rail" aria-label={t.nav.command}><button className="railLogo" aria-label="Prometheus">P</button>{(['home','command','projects','missions','approvals','changes','agents','activity','memory','settings'] as NavigationView[]).map(v=><button key={v} aria-label={t.nav[v]} className={active===v?'active':''} onClick={()=>onSelect(v)}><Icon name={v} size={18}/></button>)}</nav>}
