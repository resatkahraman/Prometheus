import { useLocale } from '../../i18n/LocaleContext'; import type { CoreStatus } from '../../types/desktop';
export function InspectorPanel({ coreStatus }: { coreStatus: CoreStatus }) { const { t } = useLocale(); const state = coreStatus.state === 'ready' ? t.ready : coreStatus.state === 'auth_required' ? t.authRequired : coreStatus.state === 'protocol_error' ? t.protocolError : t.offline; return <aside className="inspector"><div className="eyebrow">{t.context}</div><dl><dt>{t.surface}</dt><dd>{t.center}</dd><dt>{t.transport}</dt><dd>{t.localNativeBridge}</dd><dt>{t.authority}</dt><dd>{t.loopbackOnly}</dd><dt>{t.core}</dt><dd>{state}</dd></dl><div className="eyebrow">{t.security}</div><dl><dt>{t.filesystem}</dt><dd>{t.denied}</dd><dt>{t.shell}</dt><dd>{t.denied}</dd><dt>{t.process}</dt><dd>{t.denied}</dd><dt>{t.remote}</dt><dd>{t.denied}</dd></dl></aside>; }
