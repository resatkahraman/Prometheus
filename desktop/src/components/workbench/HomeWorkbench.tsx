import { useLocale } from '../../i18n/LocaleContext';
export function HomeWorkbench({ onCommand }: { onCommand: () => void }) { const { t } = useLocale(); return <section className="home"><div className="eyebrow">{t.eyebrow}</div><h1>{t.heading}</h1><p>{t.support}</p><button className="commandSurface" onClick={onCommand}>{t.ask}<kbd>Ctrl K</kbd></button><div className="quick"><button>{t.start}</button><button>{t.open}</button><button>{t.review}</button></div><div className="system"><div><b>{t.core.toUpperCase()}</b><span>{t.notConfigured}</span></div><div className="authority"><span>{t.filesystem} <b>{t.denied}</b></span><span>{t.shell} <b>{t.denied}</b></span><span>{t.process} <b>{t.denied}</b></span><span>{t.remote} <b>{t.denied}</b></span><span>{t.authority} <b>Prometheus Core</b></span></div></div></section>; }
