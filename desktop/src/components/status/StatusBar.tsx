import { useLocale } from '../../i18n/LocaleContext';
export function StatusBar({ state }: { state: string }) { const { t } = useLocale(); const label = state === 'ready' ? t.ready : state === 'auth_required' ? t.authRequired : state === 'protocol_error' ? t.protocolError : state === 'preview' ? t.preview : state === 'connecting' ? t.commandSubmitting : t.offline; return <footer className="statusbar"><span>{t.desktopLabel}</span><span>{t.core}: {label}</span><span className="spacer" /><span>{t.safety}: {t.enforced}</span></footer>; }
